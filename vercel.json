{
    "version": 2,
    "builds": [
        {
            "src": "core_folder_name/wsgi.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "core_folder_name/wsgi.py"
        }
    ]
}
